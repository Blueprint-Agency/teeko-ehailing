// @teeko/maps — thin wrappers over react-native-maps + expo-location.
// Filled in during Phase B (booking) of the rider mockup plan.
// Intended exports: MapView, Marker, Polyline, CurrentLocationButton, useCurrentLocation.

export const MAPS_PACKAGE_VERSION = '0.1.0';
