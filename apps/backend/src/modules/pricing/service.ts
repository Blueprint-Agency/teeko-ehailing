// modules/pricing/service.ts
// Fare quotes, line-item fares, surge zones + config.
// Single source of truth for the pricing domain.
// Routes call into this service; repos stay private to the module.

export const pricingService = {
  todo: 'Fare quotes, line-item fares, surge zones + config.',
};
