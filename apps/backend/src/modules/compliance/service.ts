// modules/compliance/service.ts
// PDPA consent + audit + SAR, EVP per-vehicle, PSV, insurance.
// Single source of truth for the compliance domain.
// Routes call into this service; repos stay private to the module.

export const complianceService = {
  todo: 'PDPA consent + audit + SAR, EVP per-vehicle, PSV, insurance.',
};
