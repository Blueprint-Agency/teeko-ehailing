// modules/onboarding/service.ts
// Resumable application state, document uploads, vehicle-change tickets.
// Single source of truth for the onboarding domain.
// Routes call into this service; repos stay private to the module.

export const onboardingService = {
  todo: 'Resumable application state, document uploads, vehicle-change tickets.',
};
