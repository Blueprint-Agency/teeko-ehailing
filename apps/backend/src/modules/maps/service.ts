// modules/maps/service.ts
// Google Maps proxy: places, directions, geocoding.
// Single source of truth for the maps domain.
// Routes call into this service; repos stay private to the module.

export const mapsService = {
  todo: 'Google Maps proxy: places, directions, geocoding.',
};
