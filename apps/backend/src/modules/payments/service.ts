// modules/payments/service.ts
// Intents, captures, refunds — Stripe + TNG + GrabPay + Google Pay.
// Single source of truth for the payments domain.
// Routes call into this service; repos stay private to the module.

export const paymentsService = {
  todo: 'Intents, captures, refunds — Stripe + TNG + GrabPay + Google Pay.',
};
