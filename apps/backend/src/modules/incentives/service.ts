// modules/incentives/service.ts
// Daily/weekly campaigns, referrals, activation bonuses, ratings rollup.
// Single source of truth for the incentives domain.
// Routes call into this service; repos stay private to the module.

export const incentivesService = {
  todo: 'Daily/weekly campaigns, referrals, activation bonuses, ratings rollup.',
};
