// modules/notifications/service.ts
// FCM/SMS/email outbox, in-app inbox, device tokens, broadcasts.
// Single source of truth for the notifications domain.
// Routes call into this service; repos stay private to the module.

export const notificationsService = {
  todo: 'FCM/SMS/email outbox, in-app inbox, device tokens, broadcasts.',
};
