// modules/content/service.ts
// Versioned, locale-tagged copy: T&C, broadcasts, help, cancel reasons.
// Single source of truth for the content domain.
// Routes call into this service; repos stay private to the module.

export const contentService = {
  todo: 'Versioned, locale-tagged copy: T&C, broadcasts, help, cancel reasons.',
};
