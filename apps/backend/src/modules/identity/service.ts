// modules/identity/service.ts
// Users, roles, Auth0 sub mapping.
// Single source of truth for the identity domain.
// Routes call into this service; repos stay private to the module.

export const identityService = {
  todo: 'Users, roles, Auth0 sub mapping.',
};
