// modules/admin/service.ts
// Cross-module ops, broadcast composer, PDPA tools, force-cancel.
// Single source of truth for the admin domain.
// Routes call into this service; repos stay private to the module.

export const adminService = {
  todo: 'Cross-module ops, broadcast composer, PDPA tools, force-cancel.',
};
