// WebSocket gateway — wired in v1.0 (Socket.IO + Redis adapter).
// Namespaces: /ws/rider, /ws/driver, /ws/public/:share_token
export const wsGatewayStub = { todo: 'mount Socket.IO server in v1.0' };
